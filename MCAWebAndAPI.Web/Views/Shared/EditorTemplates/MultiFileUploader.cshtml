@model IEnumerable<HttpPostedFileBase>

@(Html.Kendo().Upload()
    .Name("Documents")
    .Multiple(true)
    .Events(e => 
            e.Upload("onUpload"))
)
@Html.ValidationMessageFor(model => model)