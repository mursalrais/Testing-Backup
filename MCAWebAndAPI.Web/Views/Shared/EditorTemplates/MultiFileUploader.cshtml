@model IEnumerable<HttpPostedFileBase>

@(Html.Kendo().Upload()
    .Name("Documents")
    .Multiple(true)
    .Events(e => 
            e.Select("onSelectMultipleFileUploader"))
)
@Html.ValidationMessageFor(model => model)